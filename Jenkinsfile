pipeline {
     agent any
     stages {
        stage("Build") {
            steps {
				echo "${WORKSPACE}"
            }
        }
        stage("Deploy") {
            steps {
            }
        }
    }
}

